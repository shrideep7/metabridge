# dbt packages this project depends on.
#
# Nothing generated here requires a package. Uncomment what you
# need and run `dbt deps` — note that dbt will not parse the
# project until every declared package is installed.
#
# packages:
#   - package: dbt-labs/dbt_utils
#     version: [">=1.1.0", "<2.0.0"]
